---
---

<section id="about" class="about-section">
  <div class="container">
    <h2 class="section-title reveal-element">Sobre Nosotros</h2>
    <div class="about-content">
      <div class="about-text reveal-element">
        <div class="text-card">
          <p>
            El Ministerio de Música LADP Huamanga está dedicado a servir a Dios y a la comunidad a través de la música y la adoración. Nuestro equipo está formado por músicos apasionados y dedicados que buscan glorificar a Dios con sus talentos.
          </p>
          <p>
            Nuestra misión es crear un ambiente de adoración genuina donde las personas puedan conectarse con Dios y experimentar Su presencia a través de la música.
          </p>
          <p>
            Servimos en diferentes actividades de la iglesia, incluyendo servicios dominicales, eventos especiales, retiros y actividades comunitarias.
          </p>
        </div>
      </div>
      <div class="about-values reveal-element" data-delay="200">
        <h3>Nuestros Valores</h3>
        <ul class="values-grid">
          <li class="value-card">
            <div class="value-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
              </svg>
            </div>
            <strong>Excelencia</strong>
            <p>Buscamos dar lo mejor en cada ministerio y servicio.</p>
          </li>
          <li class="value-card">
            <div class="value-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
              </svg>
            </div>
            <strong>Pasión</strong>
            <p>Servimos con entusiasmo y dedicación en todo lo que hacemos.</p>
          </li>
          <li class="value-card">
            <div class="value-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
              </svg>
            </div>
            <strong>Comunidad</strong>
            <p>Valoramos la unidad y el trabajo en equipo.</p>
          </li>
          <li class="value-card">
            <div class="value-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <path d="M12 16v-4"></path>
                <path d="M12 8h.01"></path>
              </svg>
            </div>
            <strong>Autenticidad</strong>
            <p>Promovemos una adoración sincera y genuina.</p>
          </li>
        </ul>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const revealElements = document.querySelectorAll('.reveal-element');
    
    const revealOnScroll = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const delay = entry.target.getAttribute('data-delay') || 0;
          setTimeout(() => {
            entry.target.classList.add('revealed');
          }, delay);
          revealOnScroll.unobserve(entry.target);
        }
      });
    }, {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    });
    
    revealElements.forEach(element => {
      revealOnScroll.observe(element);
    });
  });
</script>

<style>
  .about-section {
    padding: 5rem 0;
    background-color: #f9fafb;
    position: relative;
    overflow: hidden;
  }

  .about-section::before {
    content: '';
    position: absolute;
    top: -50px;
    right: -50px;
    width: 200px;
    height: 200px;
    border-radius: 50%;
    background-color: rgba(var(--primary-orange-rgb), 0.05);
    z-index: 0;
  }

  .about-section::after {
    content: '';
    position: absolute;
    bottom: -100px;
    left: -100px;
    width: 300px;
    height: 300px;
    border-radius: 50%;
    background-color: rgba(var(--primary-blue-rgb), 0.05);
    z-index: 0;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    position: relative;
    z-index: 1;
  }

  .section-title {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 3rem;
    color: var(--primary-blue);
    position: relative;
  }

  .section-title::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 3px;
    background-color: var(--primary-orange);
    transition: width 0.3s ease;
  }

  .section-title:hover::after {
    width: 120px;
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: start;
  }

  .text-card {
    background-color: white;
    border-radius: 16px;
    padding: 2rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
    height: 100%;
  }

  .text-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
  }

  .about-text p {
    margin-bottom: 1.5rem;
    font-size: 1.1rem;
    line-height: 1.7;
    color: #4b5563;
  }

  .about-text p:last-child {
    margin-bottom: 0;
  }

  .about-values h3 {
    font-size: 1.8rem;
    margin-bottom: 1.8rem;
    color: var(--primary-blue);
    text-align: center;
  }

  .values-grid {
    list-style: none;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .value-card {
    background-color: white;
    border-radius: 16px;
    padding: 1.5rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .value-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
  }

  .value-icon {
    background-color: var(--primary-orange);
    color: white;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1rem;
    transition: all 0.3s ease;
  }

  .value-card:hover .value-icon {
    transform: scale(1.1) rotate(5deg);
    background-color: var(--primary-blue);
  }

  .value-card strong {
    display: block;
    font-size: 1.2rem;
    margin-bottom: 0.8rem;
    color: var(--primary-blue);
  }

  .value-card p {
    margin: 0;
    color: #4b5563;
    font-size: 0.95rem;
  }

  /* Animaciones */
  .reveal-element {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.5, 0, 0, 1);
  }

  .reveal-element.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  @media (max-width: 992px) {
    .values-grid {
      gap: 1rem;
    }

    .value-card {
      padding: 1.2rem;
    }
  }

  @media (max-width: 768px) {
    .about-section {
      padding: 4rem 0;
    }

    .about-content {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .section-title {
      font-size: 2rem;
      margin-bottom: 2.5rem;
    }

    .about-values h3 {
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
    }
  }

  @media (max-width: 576px) {
    .values-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .value-card {
      padding: 1.5rem;
    }

    .value-icon {
      width: 50px;
      height: 50px;
    }

    .about-section {
      padding: 3rem 0;
    }

    .section-title {
      font-size: 1.8rem;
    }

    .about-text p {
      font-size: 1rem;
    }
  }
</style>